import 'package:flutter/cupertino.dart';

class InterruptionSyncScreen extends StatelessWidget {
  const InterruptionSyncScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
